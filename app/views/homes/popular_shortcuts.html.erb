<%= turbo_frame_tag :home_popular_shortcuts do %>
<div class="grid grid-cols-2 sm:grid-cols-4 gap-4 mb-8">
    <% @popular_shortcuts.each_with_index do |shortcut, index| %>
        <%= render partial: "ranking_shortcut", locals: { shortcut: shortcut, index: index } %>
    <% end %>
</div>
<% end %>
