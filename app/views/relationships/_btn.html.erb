<% if current_user.following?(user) %>
    <%= render partial: "relationships/unfollow_button", locals: { user: user } %>
<% else %>
    <%= render partial: "relationships/follow_button", locals: { user: user } %>
<% end %>
